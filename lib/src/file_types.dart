import 'dart:io';

class GalleryFile {
  File file;
  String filename;
  GalleryFile({
    this.file,
    this.filename,
  });
}

class GalleryUrl {
  String url;
  String filename;
  GalleryUrl({
    this.url,
    this.filename,
  });
}
